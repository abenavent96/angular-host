import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.app',
  appName: 'angular-host',
  webDir: 'dist/angular-host',
  bundledWebRuntime: false
};

export default config;
